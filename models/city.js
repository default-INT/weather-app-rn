export default class City {
    /**
     * 
     * @param {number} id 
     * @param {string} name 
     * @param {string} country 
     */
    constructor(id, name, country) {
        this.id = id;
        this.name = name;
        this.country = country;
    }
}